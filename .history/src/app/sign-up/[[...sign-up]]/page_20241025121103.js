//here you can use your clerk SignUp component
//connection with db
//auth() and userId
// from to collect the user's profile data
//sql query to insert the user's data into the database
//you can then redirect the user to the homepage once they submit profile form
export default function SignUpPage() {
  return (
    <>
      <h1>SignUp Page</h1>
    </>
  );
}
